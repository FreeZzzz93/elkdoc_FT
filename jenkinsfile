pipeline {
  agent {label 'master'}

  stages {
    stage('UP and BUILD') {
      steps {
        sh '/docker/elkdoc/docker-compose up --build'
        }
      }
    stage('show UP') {
      steps {
        sh 'docker ps -a'
        }
      }
    }
}