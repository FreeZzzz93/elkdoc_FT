pipeline {
  agent {label 'master'}

  stages {
    stage('UP and BUILD') {
      steps {
        dir ('/docker/elkdoc') {
	      	sh 'docker-compose up --build'         
        }
      }
    }
    stage('show UP') {
      steps {
        sh 'docker ps -a'
        }
      }
    }
}